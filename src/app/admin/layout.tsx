// This is a new file to apply a layout to all admin pages.
// We don't need any specific layout for now, just the children.
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
